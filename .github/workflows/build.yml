name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install pandoc and LuaTeX
        run: sudo apt-get update && sudo apt-get install -y pandoc texlive-luatex

      - name: Build manuscript
        run: python scripts/build.py
